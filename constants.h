using namespace std;

const double better_at = 1.2;   //multiplier for skills that the hero/monster is better at
const double normal_at = 1.1;   //multiplier for skills that the hero/monster is normal at
const int level_up_money = 200; // money gained for each level up 
const int length = 10;
const int height = 10;